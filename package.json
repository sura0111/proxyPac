{
  "name": "proxypacswitch",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "scripts": {
    "g:lint": "cd $INIT_CWD && eslint --ext .ts,.vue,.cjs,.json ./",
    "dev": "vite",
    "build": "vite build",
    "lint": "yarn workspaces foreach --since -pR run lint"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@crxjs/vite-plugin": "^1.0.14",
    "@packages/background": "workspace:^",
    "@packages/eslint-config": "workspace:^",
    "@packages/popup": "workspace:^",
    "@types/eslint": "^8.44.4",
    "@vitejs/plugin-vue": "^4.4.0",
    "eslint": "^8.51.0",
    "vite": "^4.4.9"
  },
  "engines": {
    "node": ">=18"
  }
}
