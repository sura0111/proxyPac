export enum KEY {
  pacs = 'pacs',
  pac = 'pac',
  systemDefault = 'System Default',
}

export enum PAGE {
  addPac = 'addPac',
  editPac = 'editPac',
  settings = 'settings',
  switcher = 'switcher',
}
